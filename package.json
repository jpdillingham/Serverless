{
  "scripts": {
    "start": "sls offline start --disableCookieValidation --useChildProcesses",
    "watch": "nodemon --watch serverless.yml"
  },
  "devDependencies": {
    "nodemon": "^2.0.11",
    "npm-watch": "^0.10.0",
    "serverless-offline": "^7.0.0"
  }
}
